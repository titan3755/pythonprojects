from app import register_patient
from app import convert_to_secs

op = input('Convert ___ into seconds? ')
value = input('Enter value: ')

convert_to_secs(value, op)


# name = input('Enter name of patient: ')
# age = input('Enter age of patient: ')
# gender = input('Is patient male? ')
# old_new = input('Is patient old or new? ')
#
# register_patient(name, age, gender, old_new)


